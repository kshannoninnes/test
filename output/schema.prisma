datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Test {
  abc String @id @unique
  age Int
}

model Practice {
  60 Int
  id Int @id @default(autoincrement()) @unique
}